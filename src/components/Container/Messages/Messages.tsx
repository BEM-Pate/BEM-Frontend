import React from 'react';
import classNames from 'classnames';
import styles from './Messages.module.scss';

const Messages = () => (
  <div className={classNames(styles.Messages)}>Messages</div>
);

export default Messages;

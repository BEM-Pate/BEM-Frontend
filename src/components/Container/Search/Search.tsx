import React from 'react';
import classNames from 'classnames';
import styles from './Search.module.scss';

const Search = () => (
  <div className={classNames(styles.Search)}>Search</div>
);

export default Search;

import React from 'react';
import classNames from 'classnames';
import styles from './Groups.module.scss';

const Groups = () => (
  <div className={classNames(styles.Groups)}>Groups</div>
);

export default Groups;

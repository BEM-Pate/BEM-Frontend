import React from 'react';
import classNames from 'classnames';
import styles from './Profile.module.scss';

const Profile = () => (
  <div className={classNames(styles.Profile)}>Profile</div>
);

export default Profile;
